let imported;let createStore;let writeOnly;let readOnly;let $writeOnly;let $readOnly;let $imported;(
() => $imported = 'clicked'
);{imported,createStore,writeOnly,readOnly,$writeOnly,$readOnly,$imported}